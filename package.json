{
  "name": "cms-college-management-system",
  "version": "1.0.0",
  "description": "College management system with student registration",
  "main": "index.js",
  "scripts": {
    "build": "echo No build step needed",
    "start": "echo Starting app"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "bcrypt": "^5.1.1",      // Add this if using native bcrypt
    "bcryptjs": "^2.4.3",    // Optional: keep if some code uses bcryptjs
    "mongoose": "^7.2.1"
  }
}
